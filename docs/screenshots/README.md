# Screenshots

Drop the README screenshots here with these exact filenames (PNG):

| File | What it shows |
| --- | --- |
| `landing.png` | Public landing page (hero + world map) |
| `dashboard.png` | Metrics dashboard (stats, sales, categories, map, promotions) |
| `login.png` | Login modal with the Admin / User demo accounts |
| `company-detail.png` | Company detail as an admin (edit/delete actions visible) |

They are referenced from the top-level [`README.md`](../../README.md). Keep them
reasonably sized (≈1600px wide) so the repo stays light.
